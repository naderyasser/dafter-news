# تقرير التنظيف والتثبيت — الدفتر نيوز

مراجعة كاملة للمستودع: استعادة العمل غير المحفوظ، صيد الانحدارات، تثبيتها
باختبارات، وبناء بوابة تمنع تكرارها.

النطاق: `fix/covers-language-and-search` @ `7c9e1da` وما بعده.

---

## 1. الخلاصة — قبل وبعد

| | قبل | بعد |
|---|---|---|
| تغييرات غير محفوظة | **194 ملفاً (+6,115/−2,290)** | **0** |
| commits غير مرفوعة | العمل كله خارج git | 16 commit أحادي الغرض |
| اختبارات الخلفية | 418 ✅ | **423 ✅** |
| اختبارات الواجهة | 846 ✅ | **864 ✅** |
| تغطية الخلفية | *غير مقيسة — لا أداة* | **84%** |
| تغطية الواجهة | *غير مقيسة — لا أداة* | **78% جُمل / 81% أسطر** |
| eslint | **لم يُشغَّل قط — لا ملف إعداد** | ✅ نظيف عند `--max-warnings=0` |
| `tsc --noEmit` | نظيف | نظيف |
| انحراف الهجرات | لا يوجد | لا يوجد (مفروض في CI) |
| CI | **لا يوجد** | GitHub Actions على كل PR |
| خطّاف ما قبل الدفع | **لا يوجد** | `make install-hooks` |
| أمر تحقق واحد | **لا يوجد** | `make check` |
| ثغرات عالية (npm) | 3 | **2** (تحتاج ترقية كبرى — مؤجلة) |

---

## 2. أخطر ما وُجد: العمل كان خارج git

`origin/main...HEAD` كان `0 0` — أي أن **شهرين من العمل لم يكونا على GitHub
إطلاقاً**، بينما الإنتاج يعمل من نفس هذه النسخة. عطل قرص واحد كان سيمحوها.

قُسِّمت إلى 16 commit أحادي الغرض، كل واحد يمر باختباراته:

| commit | المحتوى |
|---|---|
| `e45d1de` | `chore` تجاهل node_modules ومخرجات البناء وملفات .env الحقيقية |
| `c5939d2` | `refactor` نقل لوحة التحكم عن `/dashboard` |
| `8f10740` | `chore` إزالة ميزة المسودة بالذكاء الاصطناعي |
| `a2b9934` | `refactor` حذف ثلاثة مكوّنات لا تُعرض |
| `2a1d4e1` | `feat` صلاحيات حسب الدور على كل نقطة كتابة |
| `d699432` | `feat` خط أنابيب المحتوى (جدولة، أبعاد الغلاف، فلاتر) |
| `28109d4` | `fix` صورة الكاتب المرتبط + اختبار انحدار |
| `4263617` | `feat` طبقة lib المشتركة (RSS، SEO، rich-text) |
| `e262203` | `feat` لوحة التحكم — ربط الكاتب، الأدوار، المحرر |
| `5d91a80` | `feat` مكوّنات الموقع — الطوابع الزمنية، شريط سطر واحد |
| `fb33599` | `feat` الصفحات — الترتيب التحريري، منع التكرار |
| `a0a229f` | `docs` توجيه وثائق النشر إلى aldaftarnews.com |
| `cc99fbc` | `chore` أمر تحقق واحد + eslint + CI + خطّاف |
| `bc17d5d` | `refactor` مصدر واحد للون العنوان الفرعي |
| `aafa7ee` | `test` تغطية ربط الكاتب والقائمة المضغوطة |
| `1ab85f8` | `chore` ترقيع nanoid |

---

## 3. أُصلِح

### 3.1 انحدارات حقيقية (كل واحد باختبار يفشل قبل وينجح بعد)

**أ. صورة الكاتب تختفي عند وجود byline يدوي** — `28109d4`
*بلاغ من الإنتاج:* صورة «ماهر فرغلي» تظهر في صفحة مقاله ولا تظهر في كاروسيل
الرأي ولا في شبكة القسم — نفس الكاتب، نفس الصورة، ظاهرة في مكان وغائبة في
مكانين.

*السبب:* `ArticleCardSerializer.get_author_avatar` كان يكتم الصورة كلما كان
`byline` غير فارغ، نسخاً للقاعدة التي تستعملها حقول الاسم. القاعدة صحيحة
للاسم (الـbyline تجاوز متعمّد لـ«من كتب هذا»)، وخاطئة للصورة — الصورة ليست
جزءاً من التجاوز، بل هي فقط: هل رفع الحساب المرتبط صورة أم لا.

*ما جعله يختبئ:* `ArticleDetailSerializer` يضمّن `AuthorSerializer` كاملاً
ولا يستشير `byline` إطلاقاً، فصفحة المقال كانت تبدو سليمة — ومن يفحص مقالاً
واحداً لا يرى العطل.

*التثبيت:* `backend/content/tests_author_avatar.py` (5 اختبارات). يفشل واحد
منها على `d699432` وينجح على `28109d4`. يثبّت أيضاً أن تجاوز **الاسم** ما زال
يعمل، حتى لا ينزلق الإصلاح إلى العطل المعاكس.

**ب. صور مكسورة تبقى مربعات رمادية فارغة** — ضمن `5d91a80`
`CoverImage` لم يكن فيه `onError`. رابط صورة يرجع 404 لا يُطلق `onLoad` أبداً،
فتبقى الصورة عند `opacity-0` فوق الرمادي — إلى الأبد. هذا هو ما كانت عليه
«٣ صور مكسورة من ٩٤» فعلياً. مُثبَّت في `CoverImage.test.tsx`.

**ج. `useEffect` بمصفوفة اعتماديات ناقصة** — ضمن `cc99fbc`
`WelcomeToast` يستعمل `wrongEdition` دون إدراجه. المتغير مشتق من `lang` أيضاً،
فتغيّر اللغة وحده لم يكن ليعيد تقييم الحارس. خامل اليوم (ar/en مساران
منفصلان) لكنه خطأ. **وجدَه أول تشغيل لـeslint على الإطلاق.**

**د. `eslint-disable` في المكان الخطأ** — ضمن `cc99fbc`
في `RichText.tsx` كان التعليق فوق الـ`<span>` الحاوي لا فوق الـ`<img>`
المقصود، فلم يكن يُسكت شيئاً.

### 3.2 تكرار (نُسخ بدل أن يُستورد)

**لون العنوان الفرعي `#0E4B7B` مكتوب مرتين** — `bc17d5d`
`components/site/RichText.tsx` يرسم المقال المنشور، و`lib/richTextDom.ts`
يرسم محرّر لوحة التحكم — وكلاهما يحمل نسخته من نفس الحرفية لنفس الرمز
`{H|…}`. **يجب أن يتفقا**: تغيير أحدهما كان سيجعل معاينة المحرّر تخالف ما
يراه القارئ، ولا شيء كان سيلتقط ذلك. الآن كلاهما يستورد `SUBHEADING_COLOR`.

### 3.3 كود ميت (مُثبَت، لا مُفترض)

| ما حُذف | البرهان |
|---|---|
| `ai_draft.py` + مسارها + المكوّن + اختباراها + تبعية `anthropic` | صفر إشارات في backend/ وfrontend/؛ URLconf يُحلّ (248 نمطاً) |
| `MarketsSnapshot`، `MatchesRail`، `RelatedArticlesList` | صفر إشارات عبر app/ وcomponents/ وlib/ |
| `components/ui/EyeIcon.tsx` | صفر إشارات |
| 15 صفحة تحت `/dashboard` | نُقلت (git رصدها كإعادة تسمية)، والمسار في `lib/routes.ts` وحده |

---

## 4. يحتاج قراراً بشرياً

1. **ثغرتان عاليتان في Next.js — تحتاجان `next@16`.**
   `next@14.2.35` عليه 13 تنبيهاً: تسميم كاش، XSS عبر CSP nonces، وعدة
   حالات DoS. القاعدة السادسة تمنع الترقيات الكبرى في هذه الجولة. **هذه
   أهم بند مؤجل** — عدة منها تخص نشراً ذاتي الاستضافة مثل هذا. أقترح فرعاً
   خاصاً بدورة QA.

2. **`DEFAULT_PERMISSION_CLASSES = AllowAny` كافتراضي عام.**
   فحصت **كل الـ46 صنف view موجّهاً وقت التشغيل**: جميعها تحدد صلاحياتها
   صراحةً، والـ11 التي تُحلّ إلى `AllowAny` عامة بحق (تسجيل الدخول،
   التسجيل، csrf، اشتراك الإشعارات، تتبّع الزيارة). **لا تسريب حالياً.**
   لكن الافتراضي يعني أن الـviewset التالي عام ما لم يتذكر كاتبه. أقترح
   قلبه إلى `IsAuthenticated` وإضافة `AllowAny` صراحةً للـ11.

3. **العودة عن قرار سابق، بلا توثيق.**
   `8d3a30d` عنوانه «استبدال قائمة الكتّاب بـbyline يدوي — اكتب أي اسم بلا
   حاجة لحساب»، أي أن القائمة أُزيلت **عمداً**. وقد أُعيدت في هذه الجولة
   بناءً على طلبك الأخير. الاثنان الآن في الشجرة، ومن يقرأ `8d3a30d` لاحقاً
   سيظن أن القائمة غلط. القرار الحالي: **الاثنان معاً ومتنافيان** — حساب
   مسجّل للكتّاب المعروفين، وbyline حر للضيوف و«فريق التحرير».

4. **`HSTS` و`SECURE_SSL_REDIRECT` غير مضبوطين** (تنبيها `check --deploy`).
   على الأرجح مقصود لأن nginx ينهي SSL — لكن يستحق تأكيداً.

---

## 5. تُرك عمداً

| ما | لماذا |
|---|---|
| قيم hex في `TextColorToolbar` | لوحة الألوان المعروضة على الكاتب — **محتوى** يُدفن في `{c:#hex\|…}`، ليست توكنز ثيم |
| hex في `ShareRow`/`SiteFooter` | ألوان علامات طرف ثالث (فيسبوك `#1877F2`، واتساب `#25D366`، يوتيوب `#FF0000`) — ثوابت خارجية لا تُدمج في لوحة الموقع |
| `text-left`/`text-right` (4 مواضع) | ثلاثة خريطة محاذاة يختارها الكاتب (قيم فعلية مقصودة)، وواحد داخل `dir="ltr"` صريح |
| `SectionDivider` | ليس «شريط إعلان مكسور» كما وُصف في مراجعة سابقة — بل الفاصل التوقيعي (ورقة دفتر ممزقة)، و«الأعمدة الحمراء» هي علامة الهامش للعلامة التجارية |
| `next@16` | ترقية كبرى — خارج نطاق هذه الجولة (القاعدة 6) |

---

## 6. نظافة مؤكَّدة (فُحصت، لا إجراء)

- **صفر** `console.log` / `print()` / `debugger` / `pdb.set_trace()`
- **صفر** `except: pass` أو `catch {}` فارغ
- **صفر** `TODO` / `FIXME` / `HACK` / `XXX`
- **صفر أسرار** في الشجرة **أو في التاريخ** — الملفان الوحيدان المحفوظان
  هما `.env.example`
- **صفر اختبارات** متخطّاة أو لا تؤكد شيئاً
- **RTL:** 4 خصائص اتجاه فعلية فقط في كل الواجهة، وكلها مشروعة
- **صفر استيرادات معلّقة** لأي ملف محذوف
- **لا انحراف** بين النماذج والهجرات، ولا هجرة غير مطبَّقة

---

## 7. كيف نبقيها نظيفة

**الأمر الواحد قبل أي push:**

```bash
make check
```

يشغّل: انحراف الهجرات ← `tsc` ← `eslint` ← اختبارات الخلفية ← اختبارات
الواجهة ← بناء تحقق. البناء يذهب إلى `.next-verify` عبر `NEXT_DIST_DIR`
**ولا يمس `.next` الحيّة إطلاقاً** — هذا المستودع هو نسخة الإنتاج نفسها،
و`next build` عابر كان سيستبدل البندل تحت القرّاء أثناء الخدمة.

**خمس قواعد يتبناها هذا المستودع:**

1. **`make install-hooks` مرة واحدة بعد الاستنساخ.** يركّب `pre-push` الذي
   يشغّل `check-fast`. `pre-push` لا `pre-commit`: فحص يستغرق دقائق على كل
   commit يعلّم الناس `--no-verify` فقط.
2. **لا دفع مباشر إلى `main`.** كل شيء عبر PR، وCI بوابة إلزامية. سبب هذا
   التقرير أصلاً أن شهرين من العمل عاشا خارج git.
3. **ادفع يومياً.** الشجرة النظيفة ليست الهدف؛ الهدف ألا يكون العمل في
   مكان واحد فقط.
4. **commit واحد = غرض واحد.** `fix:` / `refactor:` / `test:` / `chore:` /
   `perf:`. لا تخلط إصلاحاً بإعادة هيكلة بتنسيق.
5. **كل إصلاح عطل يأتي باختبار يفشل قبله.** إن لم تستطع كتابة الاختبار
   الفاشل، فأنت لم تفهم العطل بعد.

**لا تشغّل `npm audit fix --omit=dev` هنا** — يقلّم devDependencies من
`node_modules` فتنكسر أدوات الاختبار. استعملها ثم `npm install`.
